import { ChartSelectorPipe } from './chart-selector.pipe';

describe('ChartSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ChartSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
